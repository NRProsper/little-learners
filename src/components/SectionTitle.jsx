

const SectionTitle = ({children}) => {
    return (
        <div className="px-[20px] py-[10px] font-outfit text-[18px] text-center rounded-[8px] font-medium border-2 border-black inline-block">
            {children}
        </div>
    )
}

export default SectionTitle